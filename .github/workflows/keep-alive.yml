name: Keep Alive

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping ClauseGuard
        run: curl -s -L -o /dev/null -w "%{http_code}" https://clauseguardapp.streamlit.app
