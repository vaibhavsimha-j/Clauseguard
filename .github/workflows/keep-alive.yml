name: Keep Alive

on:
  schedule:
    - cron: '*/30 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping ClauseGuard
        run: curl -s -o /dev/null https://clauseguard.streamlit.app
